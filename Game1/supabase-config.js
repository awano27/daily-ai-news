window.MARUPPU_SUPABASE_CONFIG = {
  // Vercel deployments should prefer /api/runtime-config with environment variables.
  // This file is only for static hosting or local manual testing.
  enabled: false,
  url: "",
  anonKey: "",
};
